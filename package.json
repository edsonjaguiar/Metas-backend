{
  "dependencies": {
    "@getbrevo/brevo": "^2.2.0",
    "@hono/zod-validator": "^0.7.5",
    "@trigger.dev/sdk": "4.3.0",
    "better-auth": "^1.3.30",
    "cloudinary": "^2.8.0",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.3",
    "ioredis": "^5.8.2",
    "pg": "^8.16.3",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "@trigger.dev/cli": "4.3.0",
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.5",
    "ultracite": "6.0.0"
  },
  "name": "server",
  "scripts": {
    "db:generate": "bun --env-file .env --bun drizzle-kit generate",
    "db:migrate": "bun --env-file .env --bun drizzle-kit migrate",
    "db:seed": "bun --env-file .env src/seed.ts",
    "db:seed:achievements": "bun --env-file .env src/seed-achievements.ts",
    "db:studio": "bun run --bun drizzle-kit studio",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  }
}